import { RealTimeLprData } from "../features/types";
import dayjs from "dayjs";

export const mockRealTimeLprData: RealTimeLprData[] = Array.from({ length: 2 }, (_, index) => {
  const timestamp = dayjs().add(index, "second").toISOString();

  return {
    id: 3000000 + index,
    camera_id: 10000000 + index,
    camera_uid: "5bbd43d7-4524-4172-9428-dfa01bfdc4f0",
    checkpoint_id: `1`,
    checkpoint_name: `Checkpoint_${index + 1}`,
    camera_name: `Camera_${index + 1}`,
    organization: "demo",
    country: "th",
    epoch_end: timestamp,
    epoch_start: timestamp,
    gps_latitude: (13.75 + Math.random() * 0.1).toFixed(8),
    gps_longitude: (100.5 + Math.random() * 0.1).toFixed(8),
    lane: `${index + 1}`,
    overview_image_url: `/images/mock_overview_${index + 1}.jpg`,
    overview_image_height: 1080,
    overview_image_width: 1920,
    plate: `กศ1234`,
    plate_confidence: (Math.random() * 100).toFixed(2),
    plate_image_url: `/images/mock_plate_${index + 1}.jpg`,
    plate_number: `1234`,
    plate_prefix: `กศ`,
    plate_x1: 190 + index,
    plate_x2: 290 + index,
    plate_x3: 280 + index,
    plate_x4: 180 + index,
    plate_y1: 520 + index,
    plate_y2: 525 + index,
    plate_y3: 590 + index,
    plate_y4: 585 + index,
    region_code: "th-46",
    region_confidence: (90 + Math.random() * 10).toFixed(2),
    source_image_height: 1080,
    source_image_width: 1920,
    travel_direction: Math.floor(Math.random() * 360),
    user_data: JSON.stringify({
      location: "mock-location",
      owner: "mock-owner",
      data: `mock-data-${index + 1}`,
    }),
    vehicle_body_type: "sedan",
    vehicle_body_type_confidence: (90 + Math.random() * 10).toFixed(2),
    vehicle_color: "white",
    vehicle_color_confidence: (70 + Math.random() * 30).toFixed(2),
    vehicle_image_url: `/images/mock_vehicle_${index + 1}.jpg`,
    vehicle_make: "toyota",
    vehicle_make_confidence: (20 + Math.random() * 40).toFixed(2),
    vehicle_model: "corolla",
    vehicle_model_confidence: (10 + Math.random() * 20).toFixed(2),
    vehicle_region_height: 600,
    vehicle_region_width: 600,
    vehicle_region_x: 0,
    vehicle_region_y: 150,
    is_special_plate: index % 5 === 0 ? 1 : 0, // every 5th is special
    special_plate_id: index % 5 === 0 ? 5000 + index : null,
    special_plate_uid: (5000 + index).toString(),
    plate_class: index % 3,
    plate_class_name: ["standard", "diplomatic", "government"][index % 3],
    special_plate_remark: index % 5 === 0 ? "VIP vehicle" : "",
    special_plate_owner_name: index % 5 === 0 ? `Owner_${index + 1}` : "",
    special_plate_owner_agency: index % 5 === 0 ? `Agency_${index + 1}` : "",
    camera_latitude: (13.75 + Math.random() * 0.1).toFixed(8),
    camera_longitude: (100.5 + Math.random() * 0.1).toFixed(8),
    title_name: "",
    color: "white",
    pin_background_color: "black",
    text_shadow: "",
    title_id: 1,
    first_name: "",
    last_name: "",
    person_class: 1,
    person_class_name: "",
    special_person_remark: "",
    special_person_owner_name: "",
    special_person_owner_phone: "",
    suspect_person_image: "",
    suspect_person_detect_image: "",
    detect_type: "",
  };
}).sort((a, b) => {
  return new Date(b.epoch_end).getTime() - new Date(a.epoch_end).getTime();
});
